# Redirects for Cloudflare Pages - SPA routing support

# Security redirects (place before catch-alls)
/.well-known/security.txt /security.txt 301
/admin* / 301
/.env* / 301
/.git* / 301
/node_modules* / 301

# API routes handled by Cloudflare Pages Functions (no redirects required)

# Legacy redirects if needed
/bolt/* /:splat 301
/old-path/* /new-path/:splat 301

# Note: Omit SPA fallback to avoid Cloudflare infinite loop warning.
# If needed, handle fallback in your Pages Function or framework routing.