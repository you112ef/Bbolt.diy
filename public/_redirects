# Redirects for Cloudflare Pages - SPA routing support

# API routes handled by Cloudflare Pages Functions (no redirects required)

# Handle all other routes with SSR via Cloudflare Pages Functions (no SPA fallback)
# /* /index.html 200

# Legacy redirects if needed
/bolt/* /:splat 301
/old-path/* /new-path/:splat 301

# Security redirects
/admin* / 301
/.env* / 301
/.git* / 301
/node_modules* / 301

# Force HTTPS (handled by Cloudflare automatically)
# Prevent access to sensitive files
/.well-known/security.txt /security.txt 301